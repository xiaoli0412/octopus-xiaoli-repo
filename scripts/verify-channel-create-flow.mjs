import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const scriptDir = path.dirname(fileURLToPath(import.meta.url));
const repoRoot = path.resolve(scriptDir, '..');

const formPath = path.join(repoRoot, 'web/src/components/modules/channel/Form.tsx');
const formSource = fs.readFileSync(formPath, 'utf8');

assert.match(formSource, /import \{ useTranslations \} from 'next-intl';/);
assert.doesNotMatch(formSource, /useLocale/);
assert.match(formSource, /const showManualConnectionFields = formData\.type !== ChannelType\.GithubCopilot && formData\.type !== ChannelType\.Antigravity;/);
assert.match(formSource, /const flowCopy = useMemo\(\(\) => \(\{/);
assert.match(formSource, /createFlowTitle: t\('flow\.createFlowTitle'\)/);
assert.match(formSource, /createFlowHint: t\('flow\.createFlowHint'\)/);
assert.match(formSource, /createFlowDesc: t\('flow\.createFlowDesc'\)/);
assert.match(formSource, /stepLabel:\s*\(index: number\)\s*=>\s*t\('flow\.stepLabel',\s*\{\s*index\s*\}\s*\)/);
assert.match(formSource, /basicSectionTitle: t\('flow\.basicSectionTitle'\)/);
assert.match(formSource, /keySectionTitle: t\('flow\.keySectionTitle'\)/);
assert.match(formSource, /modelSectionTitle: t\('flow\.modelSectionTitle'\)/);
assert.match(formSource, /advancedSectionTitle: t\('flow\.advancedSectionTitle'\)/);
assert.match(formSource, /baseUrlsDesc: t\('flow\.baseUrlsDesc'\)/);
assert.match(formSource, /flowCopy\.createFlowTitle/);
assert.match(formSource, /flowCopy\.basicSectionTitle/);
assert.match(formSource, /flowCopy\.keySectionTitle/);
assert.match(formSource, /flowCopy\.modelSectionTitle/);
assert.match(formSource, /flowCopy\.advancedSectionTitle/);
assert.match(formSource, /baseUrlsDesc: t\('flow\.baseUrlsDesc'\)/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-form`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-flow-card`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-basic-section`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-section-heading`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-section`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-filter`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-item-\$\{idx\}`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-trigger-\$\{idx\}`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-primary-\$\{idx\}`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-fetch-models-\$\{idx\}`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-global-fetch-models`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-model-select-dialog`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-model-select-title`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-model-search-input`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-model-select-all`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-model-select-list`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-model-option-\$\{model\}`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-model-select-cancel`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-model-select-confirm`\}/);

const createSource = fs.readFileSync(path.join(repoRoot, 'web/src/components/modules/channel/Create.tsx'), 'utf8');
assert.match(createSource, /key_management_mode: 'pooled',/);
assert.match(createSource, /key_routing_policy: 'round_robin',/);
assert.match(createSource, /keys: \[\{ enabled: true, channel_key: '', source_type: 'unknown', remark: '', allowed_models: '' \}\],/);
assert.match(createSource, /<ChannelForm[\s\S]*idPrefix="new-channel"/);
assert.match(createSource, /key_management_mode: formData\.key_management_mode,/);
assert.match(createSource, /key_routing_policy: formData\.key_routing_policy,/);
assert.match(createSource, /source_type: \(k\.source_type \?\? ''\)\.trim\(\),/);
assert.match(createSource, /allowed_models: \(k\.allowed_models \?\? ''\)\.trim\(\),/);

const toolbarSource = fs.readFileSync(path.join(repoRoot, 'web/src/components/modules/toolbar/index.tsx'), 'utf8');
assert.match(toolbarSource, /data-testid=\{`toolbar-create-trigger-\$\{toolbarItem\}`\}/);
assert.match(toolbarSource, /data-slot="toolbar-create-trigger"/);

const manualSectionMatches = formSource.match(/showManualConnectionFields/g) ?? [];
assert.ok(manualSectionMatches.length >= 2, 'manual connection sections should share the same guard');

assert.doesNotMatch(formSource, /<HelpHint>\{t\('nameHint'\)\}<\/HelpHint>/);
assert.doesNotMatch(formSource, /<HelpHint>\{t\('typeHint'\)\}<\/HelpHint>/);
assert.doesNotMatch(formSource, /<HelpHint className="size-3\.5">\{t\('baseUrlTooltip'\)\}<\/HelpHint>/);
assert.doesNotMatch(formSource, /<HelpHint>\{t\('multiKeyPanelHint'\)\}<\/HelpHint>/);
assert.match(formSource, /\{t\('keySummaryTotalLabel'\)\}/);
assert.match(formSource, /\{t\('keySummaryReadyLabel'\)\}/);
assert.match(formSource, /\{t\('keySummaryEnabledLabel'\)\}/);
assert.match(formSource, /pending: keySetupSummary\.pending/);
assert.match(formSource, /const \[keyFilter, setKeyFilter\] = useState\(''\);/);
assert.match(formSource, /const visibleFormKeys = useMemo\(\(\) => \{/);
assert.match(formSource, /const showKeyFilter = keySetupSummary\.total > 1 \|\| keyFilter\.trim\(\)\.length > 0;/);
assert.match(formSource, /\{showKeyFilter && \(/);
assert.match(formSource, /\{t\('keyFilterLabel'\)\}/);
assert.match(formSource, /\{t\('keyFilterHint'\)\}/);
assert.match(formSource, /placeholder=\{t\('keyFilterPlaceholder'\)\}/);
assert.match(formSource, /\{tDetail\('keySummaryLine', \{/);
assert.match(formSource, /matched: visibleFormKeys\.length/);
assert.match(formSource, /\{tDetail\('noKeysMatched'\)\}/);
assert.match(formSource, /<div className=\{`text-xs \$\{hasRealKey \? 'text-emerald-700 dark:text-emerald-200' : 'text-amber-700 dark:text-amber-200'\}`\}>\s*\{hasRealKey \? t\('keyPrimaryStatusReady'\) : t\('keyPrimaryStatusPending'\)\}/s);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-primary-\$\{idx\}`\} className="space-y-3 rounded-2xl border border-primary\/20 bg-primary\/5 p-4"/);
assert.match(formSource, /data-slot="channel-key-summary-banner"/);
assert.match(formSource, /data-slot="channel-key-summary-grid" className="flex flex-wrap items-center gap-1\.5"/);
assert.match(formSource, /const sourceTypeSummary = k\.source_type && k\.source_type !== 'unknown'\s*\? formatSourceTypeLabel\(k\.source_type\)\s*:\s*t\('sourceTypePlaceholder'\);/s);
assert.match(formSource, /\{hasRealKey \? t\('keySetupReady'\) : t\('keySetupPending'\)\}/);
assert.match(formSource, /!hasRealKey \? \(/);
assert.match(formSource, /\{t\('keyCollapsedPendingHint'\)\}/);
assert.match(formSource, /<ChevronDown className="h-4 w-4" \/>/);
assert.match(formSource, /\{t\('keyPrimarySectionTitle'\)\}/);
assert.match(formSource, /\{t\('keyValueLeadHint'\)\}/);
assert.match(formSource, /\{hasRealKey \? t\('keyPrimaryStatusReady'\) : t\('keyPrimaryStatusPending'\)\}/);
assert.match(formSource, /\{t\('keyAdvancedSectionTitle'\)\}/);
assert.match(formSource, /\{t\('keyAdvancedOptionalBadge'\)\}/);
assert.match(formSource, /\{t\('allowedModelsPerKeyLead'\)\}/);
assert.match(formSource, /const \[keyModelDrafts, setKeyModelDrafts\] = useState<Record<number, string>>\(\{\}\);/);
assert.match(formSource, /const handleAddModelToKey = \(keyIndex: number\) => \{/);
assert.match(formSource, /const handleKeyModelDraftChange = \(keyIndex: number, value: string\) => \{/);
assert.match(formSource, /const handleKeyModelDraftKeyDown = \(event: React\.KeyboardEvent<HTMLInputElement>, keyIndex: number\) => \{/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-model-draft-\$\{idx\}`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-model-add-\$\{idx\}`\}/);
assert.match(formSource, /placeholder=\{t\('modelCustomPlaceholder'\)\}/);
assert.match(formSource, /xl:grid-cols-\[minmax\(0,1\.24fr\)_minmax\(300px,0\.76fr\)\]/);
assert.match(formSource, /2xl:grid-cols-\[minmax\(0,1\.32fr\)_minmax\(320px,0\.72fr\)\]/);
assert.match(formSource, /\{!isClassifiedMode && \(/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-fetch-models-\$\{idx\}`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-model-draft-\$\{idx\}`\}/);
assert.match(formSource, /data-testid=\{`\$\{idPrefix\}-key-model-add-\$\{idx\}`\}/);
assert.match(formSource, /<HelpHint className="size-3\.5">\{t\('keyManagementModeHint'\)\}<\/HelpHint>/);
assert.match(formSource, /<HelpHint className="size-3\.5">\{t\('keyRoutingPolicyHint'\)\}<\/HelpHint>/);
assert.match(formSource, /<HelpHint className="size-3\.5">\{t\('routeTargetHint'\)\}<\/HelpHint>/);
assert.match(formSource, /<div className="max-w-md text-xs leading-5 text-muted-foreground">\{t\('modelNoSelectedHint'\)\}<\/div>/);

for (const localeName of ['zh-Hans', 'zh-Hant', 'en', 'ja']) {
    const localeSource = fs.readFileSync(path.join(repoRoot, `web/public/locale/${localeName}.json`), 'utf8');
    assert.match(localeSource, /"providerPresetHint":/);
    assert.match(localeSource, /"nameHint":/);
    assert.match(localeSource, /"typeHint":/);
    assert.match(localeSource, /"connectionSectionHint":/);
    assert.match(localeSource, /"multiKeyEntryHint":/);
    assert.match(localeSource, /"keyFilterLabel":/);
    assert.match(localeSource, /"keyFilterHint":/);
    assert.match(localeSource, /"keyFilterPlaceholder":/);
    assert.match(localeSource, /"keyCollapsedPendingHint":/);
    assert.match(localeSource, /"keyCollapsedReadyHint":/);
    assert.match(localeSource, /"keySetupPending":/);
    assert.match(localeSource, /"keySetupReady":/);
    assert.match(localeSource, /"keyPrimarySectionTitle":/);
    assert.match(localeSource, /"keyPrimarySectionDesc":/);
    assert.match(localeSource, /"keyPrimaryStatusPending":/);
    assert.match(localeSource, /"keyPrimaryStatusReady":/);
    assert.match(localeSource, /"keySummaryTotalLabel":/);
    assert.match(localeSource, /"keySummaryReadyLabel":/);
    assert.match(localeSource, /"keySummaryEnabledLabel":/);
    assert.match(localeSource, /"keySummaryPendingLabel":/);
    assert.match(localeSource, /"keyAdvancedSectionTitle":/);
    assert.match(localeSource, /"keyAdvancedSectionDesc":/);
    assert.match(localeSource, /"keyAdvancedOptionalBadge":/);
    assert.match(localeSource, /"modelNoSelectedHint":/);
    assert.match(localeSource, /"autoGroupHint":/);
    assert.match(localeSource, /"keyValueLeadHint":/);
    assert.match(localeSource, /"flow": \{/);
    assert.match(localeSource, /"createFlowTitle":/);
    assert.match(localeSource, /"basicSectionTitle":/);
    assert.match(localeSource, /"keySectionTitle":/);
    assert.match(localeSource, /"modelSectionTitle":/);
    assert.match(localeSource, /"advancedSectionTitle":/);
    assert.match(localeSource, /"baseUrlsDesc":/);
}

console.log('channel-create-flow verification passed');
