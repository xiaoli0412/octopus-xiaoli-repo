# UI 主线任务（2026-04-30）

## 唯一目标

以 `88eb9cc` 作为唯一 UI 基线，把当前前端恢复到原版壳子、顺序、留白和密度，再只按 2026-04-30 这轮浏览器批注做最小必要修改，确保运行态页面直接体现结果。

## 当前硬约束

- 首页保持原版顺序 `Total -> Activity -> StatsChart -> Rank`，并补 token / 渠道用量，不额外发明新首页结构。
- `Group` 与 `Model/Price` 桌面端按本轮新批注收敛为 3 栏；首页与渠道概括页仍是单栏例外。
- `Setting` 继续保留原版双栏瀑布流。
- `Channel` 二级菜单里的 `stats/overview` 必须单栏。
- `AI Automation` 不推倒重做，但纳入本轮文案、布局、多语言和滚动策略精修。
- 每一批前端改动后，都必须同步 `web/out -> static/out`，不允许“源码变了但运行页没变”。

## 本轮执行重点

1. 修主页、渠道、分组、模型/价格的布局、对齐和中文化显示。
2. 修 AI 自动化页的翻译 key 泄漏、长说明、左右失衡和展开空白，统一改成客观短句与卡内滚动。
3. 新增分组模式 `AI 动态`，按运行时评分直接参与候选排序，不回写 `group_items`、`priority`、渠道或 key 配置。
4. 在渠道概括页补单栏 `AI 动态` 运维区块，并保持单栏密排。
5. 继续修设置页入口问题：语言切换、`CC Switch`、冗长说明压缩、`Model Probe` 卡内滚动搜索。
6. 把旧需求文档继续降级到 `docs/archive` 背景参考，不再作为当前实现指令源。

## 文档口径

- 本文件是当前唯一主线任务文档。
- `docs/MANUAL_FRONTEND_CHECKLIST.zh-CN.md` 是当前唯一前端验收清单。
- `docs/archive` 下的旧状态、旧需求、旧 worklog 只作背景参考，不再直接驱动实现。
