# 2026-04-24 Phase G UI Screenshot Priority And Help Copy Compression

## 1. 任务信息

- 任务名称：15 张图片问题池并入文档与主线，优先处理帮助信息减字收缩与渠道/设置关键整改
- 日期：2026-04-24
- 当前阶段：Phase G
- 对应 milestone：图片问题优先返工窗口

## 2. 开工前输入

- Master plan aligned before coding (yes/no): yes
- 对应 canonical 章节：第 9 节，尤其是 `9.1`、`9.2`、`9.5`、`9.6`、`9.7`
- 对应 workflow 章节：`1.0`、`1.2`、`1.4`、`1.4.1`
- 上一个相关 worklog：`2026-04-24-phase-g-channel-create-browser-smoke-hookup.md`
- 本次任务目标：先把 15 张图片统计、优先级和验收口径写回现有 MD，再优先处理帮助信息减字收缩，并启动渠道多 Key / 每 Key 搜索模型 / 设置页默认渲染收缩整改
- 本次已盘点本地资源：canonical plan、用户上下文总账、详细工作流、前端主线状态、现有 worklog、`ChannelForm`、`GroupEditor`、`ModelProbe`、`CircuitBreaker`、`Backup` 代码与 locale
- 本次使用的本地 resources / skills / 记忆上下文：当前线程里的 15 张图片分析结论、前一轮代码落点分析、现有 no-browser 验证脚本命名约定
- 若未使用部分本地资源或上下文，原因：本轮优先聚焦图片问题池与高优先级前端模块，未展开到全部后端子域
- 本次是否启用子 agent 与分工边界：否
- 本次子 agent 使用模型：无
- 本次是否由 Codex 自动化链路或分目录 agent 协作执行：由 Codex 主线程串行推进
- 若使用分目录 agent，负责目录与禁止越界范围：无
- 若未使用子 agent，原因：当前关键路径集中在同一批文档和前端文件，主线程直接收口更快

## 3. 本次硬规则

- 所有 15 张图片统计、问题优先级、整改顺序必须正式写回现有 MD 与工作流。
- 帮助提示与介绍信息减字收缩必须作为关键整改点优先处理，不得继续作为附带优化项。
- 渠道多 Key 的整改优先保证 `统一池 / 分类 K` 双形态、每 Key 搜索模型和请求链路正确性。
- 设置页整改优先减少默认渲染和硬文本，不先追求装饰性变化。

## 4. 本次禁止事项

- 不得只在聊天里给结论而不回写现有 MD。
- 不得把“帮助提示增加了”当成完成，必须同时满足减字与不抢交互区两个要求。
- 不得在 `分类 K` 逻辑里继续复用“第一把可用 Key”的模型搜索请求方式。

## 5. 本次验收条件

- 15 张图片的统计、优先级和自动化继承规则已写入现有 MD 与工作流。
- `需求 74` 已被上提为关键整改点，并明确要求减少硬文本。
- 至少启动一个实际前端整改闭环，优先是渠道多 Key / 每 Key 搜索模型 / 设置页默认渲染收缩之一。

## 6. 本次回滚点

- 文档回滚点：本次新增条目与优先级描述块。
- 前端回滚点：`ChannelForm`、`ModelProbe`、相关 locale 的本轮补丁。

## 7. 实现范围

- 先改数据语义还是先改 UI：先同步文档语义，再改前端 UI/交互
- 受影响后端模块：暂无直接变更目标
- 受影响前端模块：`channel/Form.tsx`、`setting/ModelProbe.tsx`、相关 locale，后续可能扩展到 `group/Editor.tsx`、`setting/Backup.tsx`
- 受影响接口：`/api/v1/channel/fetch-model` 的前端调用方式
- 是否影响旧数据：否
- 是否影响旧行为：是，主要影响渠道表单交互和设置页默认展示方式

## 8. 实施步骤

1. 把 15 张图片问题统计、优先级和自动化继承规则写入现有 MD。
2. 把“帮助信息减字收缩”上提成关键整改点并写入主规划、工作流、前端主线状态。
3. 优先改 `ChannelForm`，拆出 `统一池 / 分类 K` 双形态并补每 Key 搜索模型链路。
4. 改 `ModelProbe` 默认渲染策略，减少首屏硬文本和全量模型输出。
5. 继续收缩 `Backup / Group / CircuitBreaker` 的正文说明块与异常态文字洪泛。

## 9. 测试与验证

- 构建命令：待本轮代码补丁后执行
- 测试命令：待本轮代码补丁后执行
- 专项验证：文档同步核对、渠道多 Key 手工流程、设置页默认渲染与帮助文本收缩核对

## 10. 风险与兼容性

- 新风险：帮助文本减字后如果没有保留必要语义，可能导致解释不足
- 兼容性风险：渠道表单双形态切换若处理不当，可能影响现有 pooled/classified 的输入体验
- 是否阻塞下一任务：否，本轮就是为后续连续整改扫清优先级和口径阻塞

## 11. 收工记录

- 构建是否通过：待更新
- 测试是否通过：待更新
- 本次使用了哪些本地资源 / skills / 记忆上下文：15 张图片分析、canonical plan、用户上下文总账、详细工作流、前端主线状态、现有前端代码落点
- 本次使用的本地资源 / skills / 记忆上下文分别提供了什么结论：待更新
- 本次使用了哪些子 agent 及其结论：未使用
- 子 agent 分工、负责范围与产出摘要：无
- 手工 smoke 状态：待更新
- 手工 smoke 阻塞原因 / 缺少的环境：待更新
- 待验证页面清单：渠道创建/编辑、分组编辑、设置页模型探测、备份导入、首页
- 若未使用子 agent，原因：当前关键路径集中，主线程直接推进更快
- worklog 是否更新：是
- 遗留项：待在代码补丁后继续更新
- 下一任务前置条件是否满足：满足
